# Dataset-aware entropy-maximized active learning for machine-learned interatomic potentials

- ID: `2605.20384v1`
- Published: 2026-05-19T18:33:13Z
- Updated: 2026-05-19T18:33:13Z
- Authors: Meiyan Wang, Rishi Rao, Li Zhu
- Link: https://arxiv.org/abs/2605.20384v1
- Tags: molecular_dynamics
- Relevance score: 7.0333

## 中文速览
- 做了什么: 该论文解决机器学习原子间势函数训练数据生成效率低下的问题，在当前MLIP需要大量高质量DFT标记数据但计算成本昂贵的背景下提出。现有方法多采用随机分子动力学采样，无法有效覆盖构型空间且存在数据冗余问题。该工作创新点在于结合局部熵驱动分子动力学与全局数据集感知过滤，通过双重协方差模式实现高效的主动学习框架。
- 方法: 该方法基于主动学习框架结合分子动力学模拟，使用预训练的Allegro-OAM-L基础模型作为起点。核心理论框架包含局部熵项偏置MD轨迹向结构多样性快照，以及全局熵测量通过指纹协方差矩阵的对数行列式选择提供新信息的构型。采用双重协方差模式：无序结构使用per-atom模式，有序相使用per-config模式以实现构型空间的广泛覆盖。关键技术包括基于高斯重叠矩阵特征值的解析指纹梯度计算。训练集规模控制在10²到10³个熵选择的DFT标记结构范围内。在三个不同键合类型系统上验证：碳（共价键）、硅（共价/金属键）和NaCl（离子键），涵盖压力驱动的相变过程。
- 关键结果: 在训练集大小N=800时，熵驱动采样相比随机分子动力学采样在三个系统的分布内测试集上实现能量平均绝对误差降低约3到10倍。最终训练的领域特定势函数在同分布测试数据上达到接近或低于meV/atom的精度水平。通过三次独立训练集抽取的对比实验验证，性能提升幅度取决于键合类型和随机MD基线的饱和规模。该方法在N=100到800的训练集规模范围内均表现出显著优势。
- 对你的意义: 该论文的熵驱动采样策略可直接应用于我的金属疲劳和拉伸变形模拟中的MLIP训练数据生成，特别是双重协方差模式概念可用于处理金属材料中有序晶格与缺陷区域的不同采样需求。Allegro-OAM-L基础模型的使用经验和解析指纹梯度计算方法可为我的多物理耦合场模拟提供高效的势函数开发路径。训练集规模控制在10²-10³量级的策略对于我的相场晶体模型验证具有重要参考价值，可显著降低DFT计算成本。全局数据集感知过滤机制可借鉴用于优化我的XPFC模型参数拟合过程中的训练数据选择。

## 关键词与解释
- molecular dynamics (score=4.15, hits=2): 基于原子相互作用的时间演化模拟，用于揭示微观机理与参数敏感性。
- interatomic potential (score=2.35, hits=2): 原子势函数决定 MD 模拟精度与可迁移性，是材料模拟的关键基础。
- metal (score=1.70, hits=1): 该关键词是论文核心技术要素，建议结合方法与结果章节提取可复现实验/仿真设置。

## 研究侧重点
- 分子动力学与原子尺度机制
- Molecular Dynamics（主题归类）

## 课题组模板拆解
- 研究问题: 该论文解决机器学习原子间势函数训练数据生成效率低下的问题，在当前MLIP需要大量高质量DFT标记数据但计算成本昂贵的背景下提出。现有方法多采用随机分子动力学采样，无法有效覆盖构型空间且存在数据冗余问题。该工作创新点在于结合局部熵驱动分子动力学与全局数据集感知过滤，通过双重协方差模式实现高效的主动学习框架。
- 模型设置: 该方法基于主动学习框架结合分子动力学模拟，使用预训练的Allegro-OAM-L基础模型作为起点。核心理论框架包含局部熵项偏置MD轨迹向结构多样性快照，以及全局熵测量通过指纹协方差矩阵的对数行列式选择提供新信息的构型。采用双重协方差模式：无序结构使用per-atom模式，有序相使用per-config模式以实现构型空间的广泛覆盖。关键技术包括基于高斯重叠矩阵特征值的解析指纹梯度计算。训练集规模控制在10²到10³个熵选择的DFT标记结构范围内。在三个不同键合类型系统上验证：碳（共价键）、硅（共价/金属键）和NaCl（离子键），涵盖压力驱动的相变过程。
- 模型配置参数: 尺寸/样本规模: Combined with a pre-trained founda- tion model (Allegro-OAM-L) and analytical fingerprint gradients from Gaussian overlap matrix eigenvalues, the framework produces hi...；势函数/模型版本: Entropy-maximized sampling [18] addresses this by adding a diversit...
- 主要结论: 在训练集大小N=800时，熵驱动采样相比随机分子动力学采样在三个系统的分布内测试集上实现能量平均绝对误差降低约3到10倍。最终训练的领域特定势函数在同分布测试数据上达到接近或低于meV/atom的精度水平。通过三次独立训练集抽取的对比实验验证，性能提升幅度取决于键合类型和随机MD基线的饱和规模。该方法在N=100到800的训练集规模范围内均表现出显著优势。

## PDF 正文参数证据（方法/实验）
- 解析状态: ok
- 解析来源: pypdf

- 尺寸/样本规模: Combined with a pre-trained founda- tion model (Allegro-OAM-L) and analytical fingerprint gradients from Gaussian overlap matrix eigenvalues, the framework produces hi...
- 势函数/模型版本: Entropy-maximized sampling [18] addresses this by adding a diversity-driving term to the potential energy surface, but the origi- nal per-configuration formulation suf...
- 势函数/模型版本: Uncertainty-driven dynamics (UDD) [23] directly modifies 2 the potential energy surface to bias sampling toward high-uncertainty regions.
- 势函数/模型版本: The exploration phase still depends on the base potential, but the dataset-selection criterion does not require ensemble training, committee disagreement, or model-spe...
- 边界条件: For each atomiin a periodic cell, the fingerprint is constructed by (1) collecting all neighborsjwithin a cutoff radiusr c, (2) building the overlap matrix Oab =  4πα...
- 热力学条件: 2Phonon dispersions of (a) diamond and (b) graphite at zero pressure.
- 网格/离散: In each case, the random baseline uses standard NVT Langevin MD with pure MLIP forces (k= 0, no entropy bias), exploring the same condition grid of temperatures, pre-s...
- 尺寸/样本规模: The entropy-driven energy MAE decreases monotonically withN(2.49→2.09→1.59→1.32 meV/atom), while the random-MD energy MAE is essentially flat inN(4.17→3.81 meV/atom;
- 网格/离散: 14 The Si data efficiency ratio is the smallest among the three systems studied, reflect- ing that for this comparatively well-behaved single-element covalent system r...
- 尺寸/样本规模: Ntrain Energy (meV/atom) Force (meV/ ˚A) Stress (GPa) Entropy Random Entropy Random Entropy Random 100 2.49±0.15 4.17±0.60 25.7±0.6 25.8±2.3 0.39±0.04 0.54±0.03 250 2....

- 方法/实验章节片段:
  Dataset-aware entropy-maximized active learning for machine-learned interatomic potentials Meiyan Wang, Rishi Rao, Li Zhu Department of Physics, Rutgers University, Newark, New Jersey 07102, USA. *Corresponding author(s). E-mail(s): li.zhu@rutgers.edu; Abstract We present an active learning framework for efficiently generating training data for machine-learned interatomic potentials (MLIPs). The method combines local entropy-driven molecular dynamics with global dataset-aware filtering: a per...

## Structured Summary
1) 研究问题与背景

该论文解决机器学习原子间势函数训练数据生成效率低下的问题，在当前MLIP需要大量高质量DFT标记数据但计算成本昂贵的背景下提出。现有方法多采用随机分子动力学采样，无法有效覆盖构型空间且存在数据冗余问题。该工作创新点在于结合局部熵驱动分子动力学与全局数据集感知过滤，通过双重协方差模式实现高效的主动学习框架。

2) 核心方法与模型细节

该方法基于主动学习框架结合分子动力学模拟，使用预训练的Allegro-OAM-L基础模型作为起点。核心理论框架包含局部熵项偏置MD轨迹向结构多样性快照，以及全局熵测量通过指纹协方差矩阵的对数行列式选择提供新信息的构型。采用双重协方差模式：无序结构使用per-atom模式，有序相使用per-config模式以实现构型空间的广泛覆盖。关键技术包括基于高斯重叠矩阵特征值的解析指纹梯度计算。训练集规模控制在10²到10³个熵选择的DFT标记结构范围内。在三个不同键合类型系统上验证：碳（共价键）、硅（共价/金属键）和NaCl（离子键），涵盖压力驱动的相变过程。

3) 关键结果与定量结论

在训练集大小N=800时，熵驱动采样相比随机分子动力学采样在三个系统的分布内测试集上实现能量平均绝对误差降低约3到10倍。最终训练的领域特定势函数在同分布测试数据上达到接近或低于meV/atom的精度水平。通过三次独立训练集抽取的对比实验验证，性能提升幅度取决于键合类型和随机MD基线的饱和规模。该方法在N=100到800的训练集规模范围内均表现出显著优势。

4) 对我研究的直接价值

该论文的熵驱动采样策略可直接应用于我的金属疲劳和拉伸变形模拟中的MLIP训练数据生成，特别是双重协方差模式概念可用于处理金属材料中有序晶格与缺陷区域的不同采样需求。Allegro-OAM-L基础模型的使用经验和解析指纹梯度计算方法可为我的多物理耦合场模拟提供高效的势函数开发路径。训练集规模控制在10²-10³量级的策略对于我的相场晶体模型验证具有重要参考价值，可显著降低DFT计算成本。全局数据集感知过滤机制可借鉴用于优化我的XPFC模型参数拟合过程中的训练数据选择。

5) 可复现性评估与阅读建议

论文提供了基本的方法框架和关键参数范围，但摘要未提及具体的MD时间步长、温度控制方案、DFT计算设置等关键技术细节，需查阅全文获取完整实现参数。建议重点精读方法部分的熵计算公式推导、双重协方差模式的具体实现，以及结果部分三个材料系统的详细对比分析。

## Raw Abstract
We present an active learning framework for efficiently generating training data for machine-learned interatomic potentials (MLIPs). The method combines local entropy-driven molecular dynamics with global dataset-aware filtering: a per-configuration entropy term biases MD trajectories toward structurally diverse snapshots, while a global entropy measure, the log-determinant of the fingerprint covariance matrix of the entire dataset, selects only those configurations that provide genuinely new information. We employ dual covariance modes (per-atom for disordered structures and per-config for ordered phases) to achieve broad coverage of configuration space. Combined with a pre-trained foundation model (Allegro-OAM-L) and analytical fingerprint gradients from Gaussian overlap matrix eigenvalues, the framework produces high-quality domain-specific potentials with near- or sub-meV/atom accuracy on test data drawn from the same distribution at training-set sizes of order $10^{2}$ to $10^{3}$ entropy-selected DFT-labeled structures. We demonstrate the method on three systems spanning diverse bonding types and pressure-driven phase transitions: carbon (covalent), silicon (covalent/metallic), and NaCl (ionic). In learning curve comparisons against random molecular dynamics sampling at matched training set sizes ($N = 100$ to $800$), evaluated over three independent training-set draws per condition, entropy-driven sampling achieves a factor of approximately $3$ to $10$ lower energy MAE at $N = 800$ on in-distribution holdouts across the three systems, with the magnitude of the gain depending on the bonding type and the size at which the random-MD baseline saturates.
